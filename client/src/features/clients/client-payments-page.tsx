import { WorkspaceLayout } from "@/components/workspace-layout";
import { clientFeatureRegistry } from "@/core/registry-index";
import { formatDA, normalizeBundle, type ClientDraft } from "@/lib/client-data";
import { trpc } from "@/lib/trpc";
import { ArrowDownRight, ArrowLeft, ArrowUpRight, Check, Landmark, Save, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Link } from "wouter";

const clientTabs = clientFeatureRegistry;

export function ClientPaymentsPage({ clientId }: { clientId: number }) {
  const query = trpc.clients.get.useQuery({ clientId });
  const financeInput = useMemo(() => ({ clientId }), [clientId]);
  const entriesQuery = trpc.cabinetFinance.list.useQuery(financeInput);
  const utils = trpc.useUtils();
  const [draft, setDraft] = useState<ClientDraft | null>(null);
  useEffect(() => { if (query.data) setDraft(normalizeBundle(query.data)); }, [query.data]);
  const save = trpc.clients.saveBundle.useMutation({ onSuccess: () => void utils.clients.get.invalidate({ clientId }) });
  const entries = (entriesQuery.data ?? []).filter(entry => entry.category === "Paiement");
  if (!draft) return <WorkspaceLayout><div className="h-64 rounded-2xl border border-[#d5dfdc] bg-white/75" aria-busy="true" /></WorkspaceLayout>;
  const incoming = entries.filter(entry => entry.direction === "Entrée").reduce((sum, entry) => sum + Number(entry.amount), 0);
  const outgoing = entries.filter(entry => entry.direction === "Sortie").reduce((sum, entry) => sum + Number(entry.amount), 0);
  const paid = entries.filter(entry => entry.category === "Paiement").reduce((sum, entry) => sum + (entry.direction === "Entrée" ? Number(entry.amount) : -Number(entry.amount)), 0);
  const finalBalance = Number(draft.client.initialBalance ?? 0) - paid;
  return <WorkspaceLayout><div className="mb-7"><Link href="/clients" className="inline-flex items-center gap-2 text-sm font-bold text-[#0f766e]"><ArrowLeft size={16} /> Dossiers clients</Link><div className="mt-4 flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="text-[11px] font-extrabold uppercase tracking-[0.14em] text-[#0f766e]">Fiche client · Réf. {draft.client.referenceNumber ? String(draft.client.referenceNumber).padStart(3, "0") : "—"}</p><h1 className="mt-2 font-serif text-4xl text-[#102a43]">Paiements et dettes</h1></div><Link href="/finances" className="inline-flex h-10 items-center gap-2 rounded-lg border border-[#cfdad7] bg-white px-4 text-sm font-bold text-[#102a43]"><Landmark size={16} /> Gérer au cabinet</Link></div><nav className="mt-5 flex gap-1 overflow-x-auto border-b border-[#d7e0df]">{clientTabs.map(tab => <Link key={tab.slug} href={`/clients/${clientId}/${tab.slug}`} className={`whitespace-nowrap border-b-2 px-3 py-3 text-sm font-bold ${tab.slug === "paiements" ? "border-[#0f766e] text-[#0f766e]" : "border-transparent text-[#627785] hover:text-[#102a43]"}`}>{tab.label}</Link>)}</nav></div><div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_360px]"><section className="order-2 ui-sheet p-5 lg:order-1"><div className="grid gap-3 sm:grid-cols-3"><Metric icon={ArrowDownRight} label="Encaissements client" value={formatDA(incoming)} tone="teal" /><Metric icon={ArrowUpRight} label="Décaissements client" value={formatDA(outgoing)} tone="gold" /><Metric icon={WalletCards} label="Solde estimé" value={formatDA(finalBalance)} tone="navy" /></div><div className="mt-7 overflow-x-auto border border-[#d5dfdc] bg-[#fffefa]"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#f1f5ef] text-[10px] font-extrabold uppercase tracking-[0.1em] text-[#627785]"><tr>{["Date", "Nature", "Libellé", "Référence", "Document", "Sens", "Montant"].map(label => <th key={label} className="px-4 py-3">{label}</th>)}</tr></thead><tbody>{entriesQuery.isLoading ? <tr><td colSpan={7} className="px-4 py-8 text-center text-[#627785]">Préparation du registre…</td></tr> : entries.length ? entries.map(entry => <tr key={entry.id} className="border-t border-[#edf1f0]"><td className="px-4 py-3 text-[#627785]">{entry.entryDate}</td><td className="px-4 py-3"><span className="border border-[#c8dbd3] bg-[#edf4f1] px-2 py-1 text-xs font-bold text-[#0f766e]">{entry.category}</span></td><td className="px-4 py-3 font-bold">{entry.label}</td><td className="px-4 py-3 text-[#627785]">{entry.reference || "—"}</td><td className="px-4 py-3 text-xs font-semibold text-[#526872]">{entry.documentId ? "Document lié" : "—"}</td><td className={`px-4 py-3 font-bold ${entry.direction === "Entrée" ? "text-[#0f766e]" : "text-[#a1433d]"}`}>{entry.direction}</td><td className="px-4 py-3 font-extrabold">{formatDA(Number(entry.amount))}</td></tr>) : <tr><td colSpan={7} className="px-4 py-10 text-center text-sm text-[#627785]">Aucune opération liée à ce client.</td></tr>}</tbody></table></div></section><aside className="order-1 ui-sheet p-5 lg:order-2"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center border border-[#b8d2c7] bg-[#eaf3ee] text-[#0f766e]"><Landmark size={19} /></span><div><p className="ui-label">Suivi interne</p><h2 className="mt-2 font-serif text-2xl text-[#102a43]">Dettes et observations</h2><p className="mt-1 text-sm leading-5 text-[#627785]">Les mouvements de caisse restent dans Finances du cabinet. Ici, seuls les paiements et la dette de ce client sont suivis.</p></div></div><div className="mt-6 border border-[#c7d8d3] bg-[#f4faf7] p-4"><p className="ui-label">Dettes du client</p><p className="mt-3 text-xs font-bold text-[#627785]">Dette initiale</p><p className="mt-1 text-2xl font-extrabold text-[#102a43]">{formatDA(draft.client.initialBalance)}</p><p className="mt-4 text-xs font-bold text-[#627785]">Paiements nets</p><p className="mt-1 text-xl font-extrabold text-[#0f766e]">{formatDA(paid)}</p><p className="mt-4 text-xs font-bold text-[#627785]">Dette restante</p><p className={`mt-1 text-xl font-extrabold ${finalBalance > 0 ? "text-[#a1433d]" : "text-[#0f766e]"}`}>{formatDA(Math.max(finalBalance, 0))}</p></div><label className="mt-5 block text-sm font-bold text-[#30505d]">Observations financières<textarea value={draft.client.observations} onChange={event => setDraft({ ...draft, client: { ...draft.client, observations: event.target.value } })} className="mt-2 min-h-32 w-full border border-[#c7d8d3] bg-white p-3 font-normal outline-none focus:border-[#0f766e]" placeholder="Notes utiles au suivi du paiement…" /></label><button type="button" disabled={save.isPending} onClick={() => save.mutate({ clientId, data: draft })} className="mt-4 inline-flex h-11 w-full items-center justify-center gap-2 bg-[#0f766e] px-4 text-sm font-bold text-white disabled:opacity-60"><Save size={16} />{save.isPending ? "Enregistrement…" : "Enregistrer les observations"}</button></aside></div>{save.isSuccess ? <p className="mt-4 flex items-center gap-2 text-sm font-bold text-[#0f766e]"><Check size={16} /> Observations enregistrées.</p> : null}</WorkspaceLayout>;
}

function Metric({ icon: Icon, label, value, tone }: { icon: typeof Landmark; label: string; value: string; tone: "teal" | "gold" | "navy" }) { const classes = { teal: "border-t-[#0b625e] bg-[#e8f4ef] text-[#0f766e]", gold: "border-t-[#d29a3e] bg-[#fff7df] text-[#795b1d]", navy: "border-t-[#102a43] bg-[#102a43] text-white" }; return <div className={`border-t-2 p-4 ${classes[tone]}`}><Icon size={18} /><p className="mt-4 text-xs font-bold opacity-75">{label}</p><p className="mt-1 text-xl font-extrabold">{value}</p></div>; }
