# خطة تعديل Activité RC وملف العميل

## الهدف

تهدف هذه المرحلة إلى جعل اختيار نشاط **Registre de commerce** مريحًا وآمنًا على جميع أحجام الشاشات، مع إبقاء مصدر فئات وأنشطة RC محصورًا في كتالوج Excel المدمج. كما تنقل قرارَي انتساب العميل إلى **CNAS** و**CASNOS** إلى Fiche بشكل مباشر، وتحذف واجهة **Conformité** وبيانات العمل (Cases) من المسار التشغيلي دون حذف البيانات المؤرشفة أو كسر الاستيراد والتصدير القديم.

## القرارات المعتمدة

| الموضوع | القرار |
| --- | --- |
| قائمة Activité RC | تبقى قائمة بحث مخصصة، لكنها ستقيس المساحة المتاحة أسفل الحقل عند الفتح وتظهر أعلى الحقل تلقائيًا إذا لم تكفِ المساحة، مع المحافظة على التنقل بلوحة المفاتيح والإغلاق بـ Escape. |
| Catégorie RC | تستبدل القائمة العادية بمكوّن قائمة منسدلة قابل للبحث، يدعم كتابة كود الفئة مثل `101` بعد فتح القائمة، ثم الانتقال إلى النتيجة واختيارها بـ Enter أو بالنقر. لا يسمح بإضافة قيم جديدة. |
| مصدر RC | يبقى `registre-commerce-activities.ts`، المولَّد من ملف Excel المقدم، هو المصدر الوحيد للفئات والأنشطة. لا تُضاف أي قيمة من الواجهة أو من قاعدة البيانات. |
| CNAS وCASNOS | يضاف حقلان منطقيان مستقلان إلى العميل: `cnasAffiliated` و`casnosAffiliated`، قيمتهما الابتدائية `false`، ويظهر كل منهما في Fiche وفتح ملف جديد والطباعة بصيغة Oui / Non. |
| Conformité وCases | تزال من تبويبات العميل، من route النشط، ومن ملخص Réglages. تبقى الجداول والسجلات التاريخية دون حذف، وتُقرأ ملفات الاستيراد القديمة بأمان، لكن لن تعاد كتابة أو إنشاء بيانات conformité/cases جديدة عند حفظ Fiche. |
| Paiements وCaisse | لا يتغيران في هذه المرحلة؛ يظل سجل Finances du cabinet وربط العمليات بالعميل كما هو، لأن الطلب الحالي يخص فقط RC وConformité/Cases والانتسابات الاجتماعية. |

## مراحل التنفيذ

### 1. تعزيز اختيار RC

سيتم فصل حقلي **Catégorie RC** و**Activité RC** إلى مكوّنين مخصصين داخل `activity-fields.tsx`، بدلاً من إجبار `AppSelect` العام على سلوك بحث لا تحتاجه القوائم الأخرى. سيعرض Catégorie RC قائمة قابلة للفتح والنقر مع إدخال بحث داخلي يتعرف على الكود أو الاسم. بعد كتابة رقم الفئة، ستنتقل العلامة النشطة إلى أول تطابق؛ وتختار النتيجة بـ Enter، وتتغير الفئة فتُصفّر النشاط المختار كما يحدث اليوم.

سيستخدم Activité RC قياسًا في وقت الفتح لموضع الحقل وارتفاع viewport. عندما لا تتسع قائمة النتائج أسفل الحقل، ستثبت أعلى الحقل مع ارتفاع محدود وتمرير داخلي؛ وعندما تتسع، تبقى أسفله. وسيعاد الحساب عند التمرير وتغيير الحجم، مع سمات `role=listbox` و`aria-expanded` و`aria-activedescendant` كاملة لضمان الاستخدام بلوحة المفاتيح.

### 2. ترحيل البيانات المتوافق

سيُحدَّث جدول `clients` في `drizzle/schema.ts` بحقلَي boolean غير مدمّرين لـ CNAS وCASNOS مع قيم افتراضية `false`. ثم ستُنشأ migration عبر Drizzle، ويُراجع SQL الناتج قبل تطبيقه. لا تُحذف جداول `client_compliance` أو `client_work_cases` ولا صفوفها، للحفاظ على السجلات الموجودة والأرشيف.

سيُحدَّث عقد `clientInput` و`clientBundleInput`، و`ClientDraft`، و`createClientDraft`، و`normalizeBundle`، و`getClientBundle`، و`commitImport` لتبادل الحقول الجديدة. ستظل حقول conformité/cases القديمة مقبولة اختياريًا عند استيراد ملف قديم فقط، ثم تُهمل تشغيليًا بدل إعادة حفظها. حفظ Fiche لن يحذف أو يعيد إنشاء السجلات القديمة في هذين الجدولين.

### 3. تبسيط المسارات والواجهة

سيُحدَّث `clientFeatureRegistry` وmanifest التحميل و`App.tsx` لإزالة تبويب/route Conformité. سيعاد توجيه الرابط القديم `/clients/:clientId/conformite` بأمان إلى Fiche حتى لا تتحول الروابط المحفوظة إلى صفحة ميتة. ستزال صفحة Cases غير النشطة ومرجعها من المسار التشغيلي حيثما كانا ظاهرين، مع بقاء كود التوافق القديم منفصلًا إلى أن يصبح حذفه آمنًا.

سيضاف قسم صغير وواضح في Fiche، وكذلك في صفحة إنشاء العميل، بعنوان **Affiliations sociales** مع خيارين مستقلين: «Affilié à la CNAS» و«Affilié au CASNOS». ستستخدم الخيارات Oui / Non وستخزن كمعلومات العميل نفسها، لا كملاحظات conformité. ستضيف fiche المطبوعة القيمتين ضمن معلومات العميل لتظل النسخة الورقية متطابقة مع الشاشة.

### 4. تحديث السجل المركزي والملخصات

سيُحدَّث `shared/reference-registry.ts` وخدمة تتبع الاستخدام لاستبعاد بطاقات Conformité وCases من Réglages du programme، مع الإبقاء على مراجع الوثائق لأن Documents تبقى ميزة مستقلة. ستعكس البطاقات المتبقية فقط الوظائف النشطة، ويبقى كتالوج RC المحمي كما هو.

### 5. التحقق والاختبارات

سيتم اختبار اختيار فئة RC بالرقم، والاختيار بـ Enter، وإعادة تعيين النشاط عند تغيير الفئة، وقلب قائمة النشاط فوق الحقل عند ضيق المساحة. كما سيُختبر أن أي نشاط RC غير موجود في الكتالوج ما زال مرفوضًا في الخادم.

ستغطي اختبارات التكامل حفظ واستيراد وتصدير CNAS/CASNOS وعزل الحسابات، بما في ذلك رفض حساب ثانٍ لتعديل عميل لا يملكه. وستضاف حالة توافق تضمن أن حفظ Fiche جديد لا يحذف السجلات التاريخية conformité/cases. وأخيرًا ستشغل المجموعة الكاملة: `pnpm test && pnpm check && pnpm build`، ثم فحص يدوي للـ Fiche على الهاتف والكمبيوتر مع لوحة مفاتيح حقيقية.

## المخاطر والضمانات

| الخطر | المعالجة |
| --- | --- |
| فقدان بيانات conformité أو cases القديمة | لا توجد migration حذف، ولا يقوم saveBundle بحذفها أو إعادة إنشائها بعد التعديل. |
| نشاط RC خارج كتالوج Excel | يستمر التحقق الخادمي `isRegistreCommerceActivity`، ولا تظهر أي أزرار لإضافة فئة أو نشاط. |
| اختلاف الشاشة والطباعة | تستعمل الطباعة نفس `ClientDraft` وحقول الانتساب الجديدة. |
| تعطل رابط قديم لـ Conformité | يعاد توجيهه إلى Fiche المحمية بدل تركه غير صالح. |
| اختلاط الحسابات | كل قراءة أو تعديل يستمر في استخراج الحساب من الجلسة والتحقق من ملكية العميل في الخادم. |

## معايير القبول

تعد المرحلة مكتملة عندما تصبح قائمة Activité RC مرئية بالكامل فوق أو تحت الحقل بحسب المساحة، ويستطيع المستخدم فتح Catégorie RC ثم كتابة رقم فئة واختيارها بلوحة المفاتيح. تظهر CNAS وCASNOS كخيارين Oui / Non في Fiche وإنشاء العميل والطباعة، ولا تظهر Conformité أو Cases ضمن التنقل أو Réglages. وتبقى السجلات القديمة آمنة، وتنجح الاختبارات والبناء النهائيان دون أخطاء.
